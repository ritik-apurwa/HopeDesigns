import Image from "next/image";
import React from "react";
import { Father, GrandFather } from "@/components/comps/ol";
const page = () => {
  return (
    <main className="flex flex-col min-h-screen my-Box">
      <section className="h-full flex flex-col ">


      </section>
    </main>
  );
};

export default page;
